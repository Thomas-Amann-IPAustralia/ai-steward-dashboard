# Google Gemini API key for AI-powered change analysis
GEMINI_API_KEY=

# Optional: Proxy settings for scraping sites with bot detection
PROXY_HOST=
PROXY_PORT=
PROXY_USER=
PROXY_PASS=

# Nothing else is required. Thresholds, the watchlist and the model name live
# in steward_config.yaml, which is validated at startup.
